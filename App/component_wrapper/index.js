import React, {Fragment} from "react";
import Config from './../module_to_track/action/calendar';

class Tracker extends Component {
  componentDidMount () {
      const {
        
      } = this.props;
  }
  render () {
    <Fragment onClick={}>
      {this.props.children}
    </Fragment>
  }
}